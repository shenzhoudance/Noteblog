<div class="container">
  <br>
  <br>
  <%= simple_form_for @design_work do |f| %>
  <div class="group">
    <%= f.input :image, as: :file %>
  </div>
  <%= f.submit "Submit", data: { disable_with: "Submitting..." } %>
  <% end %>
</div>
